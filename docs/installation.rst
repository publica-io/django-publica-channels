============
Installation
============

At the command line::

    $ easy_install django-publica-channels

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-publica-channels
    $ pip install django-publica-channels
